# Exemplo de variáveis sensíveis do backend
JWT_SECRET=your_jwt_secret_here
DB_CONNECTION_STRING=Host=localhost;Port=5432;Database=appdb;Username=postgres;Password=postgres
# Adicione outras variáveis sensíveis do backend aqui
